interface:
  display_name: "Scientific Computing Correctness"
  short_description: "Guard scientific code against semantic drift."
  default_prompt: "Use $scientific-computing-correctness to implement, debug or validate this scientific computation from mathematical meaning through reported results."

policy:
  allow_implicit_invocation: true
